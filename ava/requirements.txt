# AVA Video Codec Testing Framework Dependencies

# Core dependencies (already available in the project)
# - encapp (from lib/encapp)
# - ava-common, ava-tests, ava-version (local modules)

# Optional dependencies for enhanced functionality
plotly>=5.0.0          # For interactive plotting and reporting
pandas>=1.3.0          # For data analysis and manipulation
numpy>=1.20.0          # For numerical computations (usually comes with pandas)

# Development dependencies (optional)
pytest>=6.0.0          # Only if you want to run the old pytest-based tests
pytest-check>=1.0.0    # Only if you want to run the old pytest-based tests
